refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} [{t}] {h({l})} {M}:{m}{n}"

root:
  level: info
  appenders:
    - stdout

loggers:
  authenticator_rs:
    level: info
